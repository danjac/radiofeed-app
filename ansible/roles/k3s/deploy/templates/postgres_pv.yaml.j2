apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-{{ k3s_postgres_major_version }}
spec:
  capacity:
    storage: {{ k3s_postgres.storage }}
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "{{ k3s_postgres.volume }}"
