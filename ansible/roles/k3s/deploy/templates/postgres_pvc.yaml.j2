apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-{{ postgres_major_version }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: {{ postgres.storage }}
