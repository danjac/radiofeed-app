apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-app
spec:
  replicas: {{ k3s_num_webapps }}
  selector:
    matchLabels:
      app: django-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: 0  # No extra pods created during update
        maxUnavailable: 1  # Only one pod can be replaced at a time
  template:
    metadata:
      labels:
        app: django-app
    spec:
      nodeSelector:
        role: webapp
      containers:
        - name: django
          image: {{ k3s_image }}
          resources:
            requests:
                memory: "{{ k3s_mem_request }}"
                cpu: "{{ k3s_cpu_request }}"
            limits:
                memory: "{{ k3s_mem_limit }}"
                cpu: "{{ k3s_cpu_limit }}"
          command: ["/bin/sh", "-c"]
          args: ["./entrypoint.sh"]
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: configmap
            - secretRef:
                name: secrets
          livenessProbe:
            httpGet:
              path: /ht/liveness-probe/
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ht/readiness-probe/
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
