apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: 1  # Only one extra pod can be created at a time
        maxUnavailable: 1  # Only one pod can be replaced at a time
  template:
    metadata:
      labels:
        app: django-scheduler
    spec:
      nodeSelector:
        role: jobrunner
      containers:
        - name: django
          image: {{ image }}
          resources:
            requests:
                memory: "{{ mem_request }}"
                cpu: "{{ cpu_request }}"
            limits:
                memory: "{{ mem_limit }}"
                cpu: "{{ cpu_limit }}"
          command: ["/bin/sh", "-c"]
          args: ["python ./manage.py scheduler"]
          envFrom:
            - configMapRef:
                name: configmap
            - secretRef:
                name: secrets
