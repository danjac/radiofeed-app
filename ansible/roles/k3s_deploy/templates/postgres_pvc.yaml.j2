apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-{{ postgres_major_version }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: {{ k3s_deploy_postgres.storage }}
