apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: loki-pv
  resources:
    requests:
      storage: {{ k3s_observability_loki_storage }}
