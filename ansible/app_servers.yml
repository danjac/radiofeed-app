- name: Manager setup
  hosts: managers
  remote_user: "{{ user }}"
  gather_facts: false
  vars_files:
    - ./vars/settings.yml
  roles:
    - managers

- name: Workers setup
  hosts: workers
  remote_user: "{{ user }}"
  gather_facts: false
  vars_files:
    - ./vars/settings.yml
  roles:
    - workers
