apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-unrestricted-egress
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - {}  # Allows unrestricted egress
