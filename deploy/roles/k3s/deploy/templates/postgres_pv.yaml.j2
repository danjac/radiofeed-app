apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-17
spec:
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "{{ postgres.volume }}"
