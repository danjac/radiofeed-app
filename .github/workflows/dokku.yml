name: dokku-deployment

on:
    workflow_call:
        secrets:
            dokku_remote_url:
                required: true
            dokku_private_key:
                required: true
jobs:
    deploy:
        steps:
            - name: Cloning repo
              uses: actions/checkout@v3
              with:
                  fetch-depth: 0
            - name: Check secret
              run: |
                  echo ${{ secrets.dokku_remote_url }}

            # - name: Push to dokku
            #   uses: dokku/github-action@master
            #   with:
            #       git_remote_url: ${{ secrets.dokku_remote_url }}
            #       ssh_private_key: ${{ secrets.dokku_private_key }}
            #       branch: main
